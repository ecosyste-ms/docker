<% @meta_title = "#{@ecosystem} | Package Usage" %>

<div class="container-sm">
  <h1><%= link_to 'Package Usage', dependencies_path %> : <%= @ecosystem %> </h1>

  <% @dependencies.each do |dependency, count| %>
    <div class="card mb-3">
      <div class="card-body">
        <h5 class="card-title">
          <%= link_to dependency, dependency_path(@ecosystem, dependency) %>
        </h5>
        
        <small class='text-muted'>
          <%= pluralize number_with_delimiter(count), 'dependent images' %> 
        </small>
      </div>
    </div>
  <% end %>

  <%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>  
</div>